<% content_for :title, t(".title") %>

<div class="w-full max-w-md">
  <h1 class="font-bold text-4xl mb-2"><%= t(".title") %></h1>
  <p class="text-gray-500 mb-6"><%= t(".subtitle") %></p>

  <%= render "form", provider_profile: @provider_profile %>
</div>
